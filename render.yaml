# In your render.yaml (must be in root folder)
services:
  - type: static
    name: todo-app
    rootDirectory: "/client"
    buildCommand: "cd client && npm install && npm run build"  # 👈 THIS WAS MISSING
    staticPublishPath: "./build"