services:
  - type: static
    name: todo-app
    rootDirectory: "/client"  # No leading dot, case-sensitive
    buildCommand: "cd client && npm install && npm run build"
    staticPublishPath: "build"  # No leading ./ or client/ prefix